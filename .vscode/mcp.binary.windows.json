{
  "servers": {
    "kubernetes-inspect": {
      "type": "stdio",
      "command": "${workspaceFolder}\\bin\\kubernetes-mcp-server.exe",
      "args": [
        "--read-only",
        "--toolsets",
        "core,config,helm"
      ],
      "envFile": "${workspaceFolder}/.vscode/mcp.env"
    }
  }
}
